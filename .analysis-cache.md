<analysis>
context: 
  - manifest.json 中 content_scripts.js 列表不包含 mermaid.min.js、katex.min.js、viz-global.js、emoji-map.js（v1.3.0 懒加载移除了它们）
  - loadScript() 使用 document.createElement('script') + document.head.appendChild(script) 注入脚本
  - Chrome Extension content script 运行在 isolated world，DOM 注入的 <script> 运行在 main world
  - renderMermaidDiagrams() 检查 typeof mermaid === 'undefined'，在 isolated world 中永远为 true
  - renderMathFormulas() 依赖 katex.render()，katex 在 isolated world 中不存在
  - loadKaTeXCSS() 检查 typeof katex === 'undefined'，会直接 reject
  - renderGraphviz() 依赖 Viz 构造函数，同样在 isolated world 中不存在
  - emoji-map.js 设置 window.emojiMap，在 main world 中设置，isolated world 中不可见
  - 所有库文件都存在于 libs/ 目录
  - web_accessible_resources 配置了 libs/* 可访问

needs: 
  - 修复懒加载机制，使脚本在 content script 的 isolated world 中可用
  - 数学公式占位符必须被正确替换为 KaTeX 渲染结果
  - Mermaid 图表必须正确渲染
  - Graphviz 和 emoji 也需要修复

key_challenges:
  - Chrome Extension content script 的 isolated world 无法访问 main world 的全局变量
  - DOM 注入的 <script> 在 main world 执行，content script 看不到

confidence: HIGH

approach: 
  将懒加载库放回 manifest.json 的 content_scripts.js 列表。
  三维评分：可维护性 5/5，健壮性 5/5，可扩展性 4/5

edge_cases:
  - loadScript 函数保留（测试引用），但实际运行中不再使用
  - reRenderMermaid() 和 reRenderContent() 中的 loadScript 调用需要同步修改
  - 内容检测逻辑保留用于跳过不必要的渲染调用

affected_scope:
  - manifest.json
  - content/content.js

execution_plan:
  - step_1: 修改 manifest.json，将 mermaid.min.js、viz-global.js、katex.min.js、emoji-map.js 加回 content_scripts.js
  - step_2: 修改 content.js init() 函数，移除 loadScript 链式调用，改为直接调用渲染函数
  - step_3: 检查并修改 reRenderMermaid() 和 reRenderContent() 中的 loadScript 调用
  - step_4: 运行测试确认修复

degradation_check: 全部通过
</analysis>
