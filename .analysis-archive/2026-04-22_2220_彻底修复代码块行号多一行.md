<analysis>
context: 
  - 上一轮修复（commit 8717be0）在 .code-block.show-line-numbers pre code 上添加了 font-size: 0; line-height: 0，在 .code-line 上恢复 font-size: 14px; line-height: 1.5
  - 但用户截图显示问题仍然存在：markdown 代码块中行号之间有明显的双倍行距
  - 当前 wrapLines 函数（content.js 第 398-427 行）将高亮代码按行包裹为 <span class="code-line">，最后用 .join('\n') 连接
  - .code-line 使用 display: block（上上次修复从 inline-block 改为 block 解决行号对齐）
  - 在 white-space: pre 的 <code> 容器中，display: block 的 <span> 之间的 \n 文本节点会产生匿名行框
  - font-size: 0; line-height: 0 方案在某些浏览器中不够可靠——匿名行框可能仍有最小高度
  - 没有其他 CSS 规则覆盖 font-size: 0，也没有 !important 规则
  - 代码复制功能使用 code.textContent，依赖 \n 来保持换行
  - .code-block pre code 基础样式：font-size: 14px; line-height: 1.5; white-space: pre
  - diff 代码块的 .code-line.diff-addition 和 .code-line.diff-deletion 已改为 display: block

needs: 
  - 彻底消除 display: block + \n 导致的双倍换行
  - 保持行号对齐（display: block 不能改回 inline-block）
  - 保持代码复制功能正常（textContent 需要包含换行符）

key_challenges:
  - font-size: 0 方案不够可靠，需要从源头消除问题
  - \n 必须存在于 DOM 中供 textContent 使用，但不能在 block 元素之间产生匿名行框
  - 解决方案：将 join('\n') 改为 join('')，同时修改复制功能遍历 .code-line

confidence: HIGH

approach: 
  方案：将 wrapLines 的 join('\n') 改为 join('')，同时修改代码复制功能从 code.textContent 改为遍历 .code-line 取 textContent 再 join('\n')。移除 CSS 中的 font-size: 0; line-height: 0 hack。为所有 .code-block pre code .code-line 添加 display: block。
  
  三维评分：
  - 可维护性: 5/5 — 从源头消除问题，不依赖 CSS hack
  - 健壮性: 5/5 — 不依赖浏览器对 font-size: 0 的特定渲染行为
  - 可扩展性: 5/5 — 未来如需调整行号样式，不需要考虑 \n 文本节点的影响

edge_cases:
  - 代码复制功能：修改为遍历 .code-line 取 textContent 再 join('\n')
  - 非行号模式下的代码块：.code-line 也需要 display: block，否则 join('') 后代码会变成一行
  - diff 代码块：已经是 display: block，不受影响
  - Mermaid 源码复制：使用 .mermaid-source code 的 textContent，不涉及 .code-line，不受影响

affected_scope:
  - f:\github\markdown_viewer_enhanced\content\content.js（wrapLines 函数 + 代码复制功能）
  - f:\github\markdown_viewer_enhanced\styles\content.css（移除 font-size hack + 添加全局 .code-line display: block）

execution_plan:
  - step_1: 修改 content.js 中 wrapLines 函数，将 join('\n') 改为 join('')
  - step_2: 修改 content.js 中代码复制功能，从 code.textContent 改为遍历 .code-line 取 textContent 再 join('\n')
  - step_3: 修改 content.css，移除 .code-block.show-line-numbers pre code 中的 font-size: 0; line-height: 0 和注释
  - step_4: 修改 content.css，移除 .code-block.show-line-numbers pre code .code-line 中的 font-size: 14px; line-height: 1.5
  - step_5: 修改 content.css，为所有 .code-block pre code .code-line 添加 display: block
  - step_6: 运行测试确认不破坏现有功能
  - step_7: 更新测试用例

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
