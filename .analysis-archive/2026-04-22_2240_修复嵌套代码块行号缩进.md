<analysis>
context: 
  - 用户的 markdown 代码块中嵌套了 ```lua 代码块，渲染后行号 7-11（嵌套代码块内容）比外层行号 1-6, 12-14 更靠右
  - hljs 对 markdown 语言的高亮会将嵌套代码块（从 ```lua 到 ```）包裹在一个跨多行的 <span class="hljs-code"> 标签中
  - wrapLines 函数按 \n 分割后逐行包裹为 <span class="code-line">，但 hljs 的跨行 <span class="hljs-code"> 标签在分割后变成了不完整的 HTML
  - 具体来说，Line 6 的 .code-line 内容是 `<span class="hljs-code">```lua`（hljs-code 打开但未关闭），wrapLines 添加的 </span> 关闭的是 hljs-code 而非 code-line
  - 这导致 Line 6 的 .code-line 没有被关闭，Line 7-11 的 .code-line 被嵌套在 Line 6 的 .code-line 内部
  - .code-line 有 padding-left: 16px，嵌套导致 Line 7-11 的实际 padding 是 16px + 16px = 32px
  - 行号 ::before 伪元素也因此向右偏移
  - wrapLines 函数当前实现（content.js 第 398-427 行）：按 \n 分割 → 逐行包裹为 <span class="code-line"> → join('')
  - .code-line 样式：display: block, padding-left: 16px（show-line-numbers 模式）
  - .code-line::before 样式：display: inline-block, width: 3em, margin-right: 16px, text-align: right

needs: 
  - 修复 hljs 跨行标签导致的 .code-line 嵌套问题
  - 确保所有行号在同一垂直线上对齐，不受代码内容中 hljs 标签的影响
  - 保持 hljs 高亮效果（颜色）不受影响
  - 保持代码复制功能正常

key_challenges:
  - hljs 的跨行 <span> 标签在按 \n 分割后会产生不完整的 HTML
  - 需要在 wrapLines 中修复跨行标签：在每行结束时关闭所有打开的标签，在下一行开始时重新打开
  - 这是一个经典的"HTML 标签平衡"问题

confidence: HIGH

approach: 
  方案：在 wrapLines 函数中添加跨行标签平衡逻辑。
  
  三维评分：
  - 可维护性: 5/5 — 标签平衡逻辑封装在 wrapLines 内部，不影响外部接口
  - 健壮性: 5/5 — 处理所有 hljs 可能产生的跨行标签场景
  - 可扩展性: 5/5 — 通用的标签平衡方案，适用于任何 hljs 语言的跨行标签

edge_cases:
  - hljs 输出中没有跨行标签的情况：openTags 栈始终为空，不影响输出
  - hljs 输出中有多层嵌套跨行标签的情况：栈正确追踪所有层级
  - 空行（无内容）的情况：openTags 仍然正确传递
  - diff 代码块的情况：diff 检测逻辑在标签平衡之后执行，不受影响
  - 代码复制功能：标签平衡只影响 HTML 结构，不影响 textContent

affected_scope:
  - f:\github\markdown_viewer_enhanced\content\content.js（wrapLines 函数）
  - f:\github\markdown_viewer_enhanced\tests\ui\line-number-align.test.js（新增测试用例）

execution_plan:
  - step_1: 修改 content.js 中 wrapLines 函数，添加跨行标签平衡逻辑
  - step_2: 运行测试确认不破坏现有功能
  - step_3: 新增测试用例验证跨行标签平衡
  - step_4: 清理临时测试文件 test-hljs.js

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
