<analysis>
context: 
  - options.html 中样式全部内联在 <style> 标签中（无独立 options.css 文件）
  - 当前有四组需要修改的按钮：
    1. 外观按钮（主题模式）：使用 .theme-selector + .theme-btn，大块卡片式按钮
    2. 目录位置：使用 .capsule-group + .capsule-btn.toc-pos-btn，纯文字高亮风格
    3. 面板模式：使用 .capsule-group + .capsule-btn.panel-mode-btn，同上
    4. 文档对齐：使用 .capsule-group + .capsule-btn.content-align-btn，同上
  - 截图目标样式：圆角边框容器，选中项绿色边框+绿色文字+白色背景，未选中项灰色文字
  - options.js 中 DOM 引用保持不变：themeBtns(.theme-btn), tocPosBtns(.toc-pos-btn), panelModeBtns(.panel-mode-btn), contentAlignBtns(.content-align-btn)

needs: 
  - 将四组按钮统一改为 segment 按钮组样式
  - 选中项：绿色边框 + 绿色文字 + 白色背景
  - 未选中项：灰色文字，无边框，透明背景
  - 整体容器：圆角边框（灰色），白色背景
  - 每个选项前有小 emoji 图标

key_challenges:
  - 主题按钮 HTML 结构需要从 block 改为 inline
  - 面板模式和文档对齐按钮需要添加 emoji
  - 不能破坏 JS DOM 选择器

confidence: HIGH

approach: 
  修改 options.html 中的 CSS 样式和 HTML 结构
  - 可维护性: 5/5
  - 健壮性: 5/5
  - 可扩展性: 5/5

edge_cases:
  - .theme-btn.active 样式需要从白色背景+阴影改为绿色边框+绿色文字
  - .capsule-group 覆盖样式需要同步更新
  - 目录位置按钮已有 emoji（📍）
  - 主题按钮文字需要简化

affected_scope:
  - f:\github\markdown_viewer_enhanced\options\options.html

execution_plan:
  - step_1: 修改 .theme-selector 和 .theme-btn CSS
  - step_2: 修改 .capsule-group 和 .capsule-btn CSS
  - step_3: 修改主题按钮 HTML
  - step_4: 修改面板模式按钮 HTML 添加 emoji
  - step_5: 修改文档对齐按钮 HTML 添加 emoji
  - step_6: 清理覆盖样式
  - step_7: 运行全量测试
  - step_8: Git commit + push

degradation_check:
  - 全部 YES，无问题
</analysis>
