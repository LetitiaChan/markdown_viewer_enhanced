
<analysis>
context: 
  - mermaid 在暗色模式下通过 mermaid.initialize() 设置 theme: 'dark' 和 themeVariables（primaryTextColor: '#e0e0e0'）
  - 当用户在 mermaid 代码中使用 style 指令自定义节点填充颜色（如 fill:#FFDEAD、fill:#ccffcc、fill:#FFB6C1），mermaid dark 主题仍将文字渲染为浅色（#e0e0e0）
  - 浅色文字在浅色自定义背景上几乎不可见
  - mermaid 渲染后的 SVG 后处理代码（content.js 2000-2060 行）只处理了尺寸适配，没有处理文字颜色
  - mermaid SVG 中的节点文字颜色是内联 style 属性，CSS 无法覆盖
  - 问题只在暗色主题下出现

needs: 
  - 在暗色主题下，当 mermaid 节点有自定义浅色填充时，自动将文字颜色设为深色确保可读性

key_challenges:
  - mermaid 生成的 SVG 中文字颜色是内联 style，必须通过 JS 后处理
  - 需要计算填充颜色的亮度来决定文字颜色

confidence: HIGH

approach: 在 mermaid SVG 渲染后添加 fixMermaidTextContrast(svgEl) 后处理函数
  三维评分：
  - 可维护性: 5/5 — 独立函数，与现有逻辑解耦
  - 健壮性: 5/5 — 基于 W3C 亮度公式，覆盖多种颜色格式
  - 可扩展性: 5/5 — 亮度阈值可调

edge_cases:
  - 渐变填充 → 跳过
  - url(#xxx) 引用 → 跳过
  - rgba 带透明度 → 解析 rgb 部分
  - 无自定义填充 → 不处理
  - 亮色主题 → 不处理
  - subgraph 标题 → 一并处理

affected_scope:
  - content/content.js
  - tests/ui/mermaid-text-contrast.test.js

execution_plan:
  - step_1: 在 content.js renderMermaidDiagrams 中 SVG 尺寸处理后调用 fixMermaidTextContrast(svgEl)
  - step_2: 实现 fixMermaidTextContrast 函数（亮度检测 + 文字颜色修正）
  - step_3: 新增测试文件

degradation_check: 全部 YES/通过
</analysis>
