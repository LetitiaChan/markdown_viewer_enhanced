<analysis>
context: 
  - mermaid 在暗色模式下通过 mermaid.initialize() 设置 theme: 'dark' 和 themeVariables
  - content.js 第 2110-2116 行定义了暗色主题变量：
    · darkMode: true
    · background: '#1e1e1e'
    · primaryColor: '#4fc3f7'
    · primaryTextColor: '#e0e0e0'
    · lineColor: '#666'
  - lineColor: '#666' 是连线颜色，在 #1e1e1e 深色背景上对比度极低
  - #666 的 RGB 为 (102, 102, 102)，相对亮度约 0.13；#1e1e1e 的 RGB 为 (30, 30, 30)，相对亮度约 0.01
  - 对比度约 2.3:1，远低于 WCAG AA 标准的 4.5:1
  - CSS 中没有对 mermaid 连线的额外样式覆盖
  - 之前已实现 fixMermaidTextContrast 函数处理节点文字对比度问题，但不涉及连线

needs: 
  - 将 mermaid 暗色主题的 lineColor 改为更亮的颜色，使连线在深色背景上清晰可见

key_challenges:
  - 修改极其简单：只需将 lineColor 从 '#666' 改为更亮的颜色值
  - 需要选择合适的颜色：既清晰又不刺眼

confidence: HIGH

approach: 将 lineColor 从 '#666' 改为 '#a0a0a0'
  '#a0a0a0' 的 RGB 为 (160, 160, 160)，与 #1e1e1e 背景的对比度约 7.5:1，满足 WCAG AA 标准
  三维评分：
  - 可维护性: 5/5 — 只修改一个颜色值
  - 健壮性: 5/5 — lineColor 是 mermaid 官方 themeVariable
  - 可扩展性: 5/5 — 未来如需调整只需改一个值

edge_cases:
  - lineColor 同时影响连线和箭头标记 → #a0a0a0 在深色背景上清晰
  - lineColor 可能影响节点边框 → #a0a0a0 作为边框也合适
  - 用户自定义连线颜色（linkStyle）→ 不受影响
  - 亮色主题 → 不受影响

affected_scope:
  - content/content.js（第 2115 行 lineColor 值）

execution_plan:
  - step_1: 修改 content.js 第 2115 行，将 lineColor 从 '#666' 改为 '#a0a0a0'
  - step_2: 运行测试确认不破坏现有功能
  - step_3: 检查现有测试是否覆盖 lineColor 配置，如无则补充测试

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
